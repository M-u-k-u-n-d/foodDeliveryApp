import {useState } from "react";

const User = (props)=>{
    return (
        <div className="">
        {/* <span>Hii, This is {props.name} </span>
        <span>A {props.role}</span> */}
        <p className="font-[cursive]">Creating a Food Delivery App using React.</p>
        </div>
    );
}

export default User;